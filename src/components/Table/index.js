import React from 'react'
import './style.css'

export default function Table({ filteredEmployees }) {
    return (
        <div>
            <tabel>
                <tr>
                <th>
                    
                </th>
                    <td>
                        {/* use .map function to map over fileteredEmployes for each user intaking props and make .row for each */}
                    </td>
                </tr>
            </tabel>
        </div>
    )
}
